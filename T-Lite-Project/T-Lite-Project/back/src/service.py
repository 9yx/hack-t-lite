import requests
import json

def run(msg):
    # url = 'http://95.174.93.97:8081/assist'
    # headers = {
    #     'Content-Type': 'application/json'
    # }
    # data = {"query": msg}
    try:
        # response = requests.post(url, headers=headers, json=data)
        # obj = json.loads(response.text)
        return "Откл. для тестирования API"
        return obj.text
    except:
        return "Ошибка, повторите позже"
    